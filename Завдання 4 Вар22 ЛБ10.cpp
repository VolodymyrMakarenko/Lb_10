#include <iostream>
#include <Windows.h>
#include <math.h>
#include <stdio.h>
#include <cmath>
using namespace std;

int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    double q, b = 0, y = 0, x = 0, t = 0, e = 0;
    q = 2;
    cout << "Варіант 22" << "\n";
    cout << "Введіть значення другого параметра: " << "\n";
    cin >> b;
    y = pow(atan(abs(x)), 2);
    cout << "y: " << y << "\n";
    x = pow(t, 3) + pow(b, 2);
    cout << "x: " << x << "\n";
    t = pow(b, 3) + pow(e, sqrt(q));
    cout << "t: " << t << "\n";



}
